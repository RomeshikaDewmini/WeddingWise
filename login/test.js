/*document.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault(); // Prevent form submission
    // Perform desired action here, such as displaying a message
    alert('Login button clicked!');
  });


var rememberCheckbox = document.querySelector('input[type="checkbox"]');
var forgetPasswordLink = document.querySelector('.remember a');

rememberCheckbox.addEventListener('change', function() {
  forgetPasswordLink.style.display = this.checked ? 'block' : 'none';
});


  document.querySelector('.footer-form').addEventListener('submit', function(event) {
    event.preventDefault(); // Prevent form submission
    // Get the entered email address
    var emailInput = document.getElementById('email');
    var email = emailInput.value;
    // Perform desired action with the email address, such as sending it to a server
    alert('Subscribed with email: ' + email);
    // Reset the form
    emailInput.value = '';
  });*/

  // // Display a message based on the validation result
  // const messageDiv = document.getElementById("message");
  // if (isValidUser) {
  //   messageDiv.innerHTML = "Login successful!";
  //   // Redirect the user to the desired page
  //   // window.location.href = "reservation.html";
  // } else {
  //   messageDiv.innerHTML = "Invalid username or password.";
  // }

  // // Clear the input fields
  // usernameInput.value = "";
  // passwordInput.value = "";
